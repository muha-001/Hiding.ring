# تحليل مشروع Hiding.ring

**المستودع:** [`muha-001/Hiding.ring`](https://github.com/muha-001/Hiding.ring)  
**تاريخ التحليل:** 29 أغسطس 2026  
**نطاق التحليل:** الفرع `main` والنسخة المستنسخة محليًا، مع فحص ملف `index.html` كاملًا واختبار صياغة JavaScript المضمّن.

## الخلاصة التنفيذية

مشروع **Hiding.ring** عبارة عن لعبة متصفح أحادية الصفحة باسم **Golden Ring: International Tournament Edition**. التطبيق يعمل دون خادم أو قاعدة بيانات أو حزمة بناء؛ إذ يحتوي المستودع على ملف `index.html` واحد يضم HTML وCSS وJavaScript، مع الاعتماد على Google Fonts وFont Awesome عبر CDN. هذا يجعل تشغيله ونشره سهلًا جدًا، لكنه يحد من قابلية الصيانة والاختبار والتوسع.

الأساس البرمجي جيد كنموذج أولي تفاعلي: توجد ثلاثة مستويات، مؤقت للمستويين المتقدمين، نظام نقاط، مؤثرات صوتية مولدة عبر Web Audio API، مؤثرات جسيمات عبر Canvas، وتصميم متجاوب. كما أن فحص الصياغة نجح دون أخطاء (`JavaScript syntax: OK`). في المقابل، لا توجد اختبارات، أو README، أو ترخيص، أو إعداد CI، أو فصل بين طبقات العرض والمنطق. أهم الملاحظات العملية هي ضعف الوصولية، الاعتماد على معالجات inline، تعطيل تكبير الصفحة على الهاتف، غياب حماية تكامل الموارد الخارجية، واحتمال إنشاء أكثر من حلقة `requestAnimationFrame` لنظام الجسيمات.

| البعد | التقييم | الملاحظة |
|---|---:|---|
| قابلية التشغيل | جيد جدًا | ملف HTML واحد ويمكن فتحه مباشرة في المتصفح |
| تجربة المستخدم | جيد | تصميم بصري قوي وتدرج واضح للمستويات |
| جودة التنظيم | متوسط | الملف كبير ومختلط الطبقات، لكنه مفهوم نسبيًا |
| الوصولية | ضعيف إلى متوسط | بطاقات اللعبة غير قابلة للوحة المفاتيح، وتكبير الصفحة معطل |
| الاختبار | ضعيف | لا توجد اختبارات آلية أو فحوص CI |
| الأمان | جيد لتطبيق محلي | لا توجد مدخلات مستخدم حساسة أو خادم؛ توجد ملاحظات على CDN و`innerHTML` |
| قابلية التوسع | ضعيف إلى متوسط | يلزم فصل الملفات والبيانات والمنطق قبل إضافة ميزات كبيرة |

## 1. نطاق المستودع وبنيته

بحسب بيانات GitHub، المستودع عام، وفرعه الافتراضي هو `main`، ولغته المصنفة HTML، ولا يحتوي على ترخيص أو موضوعات أو إصدارات منشورة. يحتوي الجذر على ملف واحد فقط هو `index.html` بحجم يقارب 39 كيلوبايت و1,156 سطرًا. ويظهر سجل Git ثلاثة تغييرات رئيسية: إنشاء البنية الأولية، تحديث الصفحة، ثم ترجمة التعليقات إلى الإنجليزية. [1] [2]

| العنصر | النتيجة |
|---|---|
| الملفات المصدرية | `index.html` فقط |
| CSS | مضمّن داخل `<style>` |
| JavaScript | مضمّن داخل `<script>` |
| الاعتماديات الخارجية | Google Fonts وFont Awesome 6.4.0 عبر CDN |
| الخادم أو قاعدة البيانات | غير موجودين |
| الاختبارات | غير موجودة |
| التوثيق | لا يوجد README في الجذر |
| الترخيص | غير محدد |

هذا التصميم مناسب للنشر السريع على GitHub Pages أو أي استضافة ملفات ثابتة. لكنه يجعل أي تعديل في الواجهة أو اللعبة تعديلًا في ملف واحد كبير، ويصعّب مراجعة التغييرات وإعادة استخدام المكونات.

## 2. الوظائف الحالية

تبدأ اللعبة بشاشة اختيار المستوى، ثم تنشئ عددًا من البطاقات عشوائيًا وتخفي محتواها. تُختار بطاقة واحدة عشوائيًا بوصفها موضع الحلقة، بينما يستطيع اللاعب تحديد بطاقة ثم قفلها أو إفراغها. عند إفراغ بطاقة غير رابحة يحصل اللاعب على نقاط، ويُعاد المؤقت إلى قيمته الكاملة في مستويي Professional وLegendary. عندما تبقى بطاقة واحدة، يُفعّل زر الكشف تلقائيًا.

| المستوى | عدد البطاقات | الزمن | مضاعف النقاط |
|---|---:|---:|---:|
| Beginner | 8 | بلا حد | ×1 |
| Professional | 12 | 45 ثانية | ×2 |
| Legendary | 16 | 30 ثانية | ×3 |

يُحسب الفوز في `revealResult()` من خلال مجموع نقاط الإفراغ ومكافأة الزمن ومكافأة عدد البطاقات التي أُفرغت. كما توجد حالات خسارة عند انتهاء الوقت أو إفراغ البطاقة الرابحة. المؤثرات الصوتية لا تعتمد على ملفات صوتية، بل تُنشئ نغمات قصيرة باستخدام مذبذبات Web Audio API، وهي فكرة مناسبة لتقليل حجم المشروع.

## 3. نقاط القوة

**بساطة النشر.** لا توجد عملية تثبيت أو بناء أو إعداد خادم؛ ويمكن تشغيل الصفحة مباشرة. هذه نقطة قوة واضحة للنموذج الأولي.

**وضوح الحالة الأساسية.** متغيرات مثل `winningHandIndex` و`selectedHandIndex` و`eliminatedHands` و`isGameOver` تجعل دورة اللعبة قابلة للتتبع. كما أن الدوال الرئيسية مقسمة إلى وحدات مفهومة نسبيًا مثل `startGame()` و`createHands()` و`startTimer()` و`revealResult()`.

**تجربة بصرية متماسكة.** استخدام لوحة ألوان موحدة، وبطاقات متدرجة، وشريط تقدم، وتأثيرات الفوز والخسارة يعطي اللعبة هوية واضحة. توجد أيضًا قواعد استجابة للشاشات الصغيرة.

**التعامل الأساسي مع دورة المؤقت.** تبدأ اللعبة المؤقت بعد اختيار مستوى زمني، وتستدعي `stopTimer()` قبل إنشاء مؤقت جديد، وتوقفه عند انتهاء اللعبة أو العودة إلى القائمة؛ وهذا يقلل خطر تكرار المؤقت بين الجولات.

**تقليل مخاطر الحقن في المحتوى الديناميكي.** النصوص الرئيسية التي تُعرض من حالة اللعبة تستخدم `textContent`. أما استخدام `innerHTML` فمحتواه ثابت ومضمّن في المصدر، وليس قادمًا من إدخال المستخدم.

## 4. الملاحظات الفنية والمخاطر

### أ. الوصولية وتجربة الاستخدام

بطاقات الأيدي ممثلة بعناصر `<div>` وبها `onclick` فقط، ولذلك لا يمكن الوصول إليها طبيعيًا عبر لوحة المفاتيح ولا تحمل دورًا دلاليًا مثل `button`. ينبغي تحويلها إلى أزرار أو إضافة `role="button"` و`tabindex="0"` ومعالجة `Enter` و`Space`، مع إضافة أسماء وصولية واضحة.

يستخدم وسم viewport القيمة `user-scalable=no`، وهو يمنع المستخدم من تكبير الصفحة على الهاتف. هذا يضر بالمستخدمين ذوي ضعف البصر، والأفضل حذف هذا الجزء والسماح بالتكبير.

الأيقونات وحدها لا توفر دائمًا معنى كافيًا لقارئات الشاشة، كما لا توجد مؤشرات واضحة للحالة مثل البطاقة المحددة أو البطاقة المُفرغة باستخدام `aria-pressed` أو `aria-disabled`. كذلك لا يظهر في المصدر دعم صريح لتفضيل `prefers-reduced-motion` رغم كثرة الحركة والوميض.

### ب. إدارة نظام الجسيمات

تستدعي `createConfetti()` ثم `showResult()` التي تستدعي `createExplosion()`. كلتا الدالتين تستدعيان `animate()` على المصفوفة نفسها، ما قد ينتج حلقتي `requestAnimationFrame` متزامنتين مؤقتًا. هذا قد يسبب تحديثات مزدوجة للوحة وزيادة غير ضرورية في استهلاك المعالج. الأفضل إضافة علم مثل `isAnimating`، بحيث توجد حلقة رسم واحدة فقط، مع دالة `clear()` لمسح الجسيمات عند بدء جولة جديدة أو العودة إلى القائمة.

### ج. الاعتماديات الخارجية

تعتمد الصفحة على Google Fonts وFont Awesome من نطاقين خارجيين. لا توجد نسخة محلية احتياطية للخطوط، ولا توجد آلية واضحة للتعامل مع فشل CDN، ولا تظهر سمات `integrity` و`crossorigin` لملف Font Awesome. في تطبيق عام، يفضّل تثبيت نسخة محددة محليًا أو إضافة fallback، واستخدام Subresource Integrity عندما يكون ذلك مناسبًا.

### د. تنظيم الكود

وجود 1,156 سطرًا في ملف واحد يخلط الهيكل والتنسيق والمنطق. في المرحلة التالية يُنصح بالفصل إلى `index.html` و`styles.css` وملفات JavaScript مثل `game-state.js` و`game-ui.js` و`audio.js` و`particles.js`. كما يُستحسن نقل تعريفات المستويات والنصوص إلى كائنات بيانات مستقلة، وإزالة معالجات `onclick` المضمنة واستخدام `addEventListener`.

### هـ. عدالة اللعبة وحدود النموذج الحالي

موضع الحلقة محفوظ في JavaScript داخل المتصفح، لذلك يستطيع أي مستخدم تقني فحص المصدر أو حالة الصفحة ومعرفة البطاقة الرابحة قبل الكشف. هذا ليس ثغرة خادم لأن اللعبة محلية ولا توجد منافسة أو جائزة، لكنه يعني أن النظام ليس مناسبًا لمسابقة تنافسية أو نتيجة يجب الوثوق بها. إذا أصبحت اللعبة تنافسية، ينبغي نقل توليد النتيجة والتحقق منها إلى خادم، أو استخدام تصميم التزام/كشف مناسب.

### و. جودة المنتج والتوثيق

لا توجد تعليمات تشغيل أو وصف للعبة أو لقطات شاشة أو سياسة مساهمة أو ترخيص. غياب الترخيص يجعل إعادة الاستخدام القانوني غير واضح، وغياب README يرفع تكلفة فهم المشروع على أي مساهم جديد. كما لا توجد اختبارات للدوال الأساسية ولا فحص HTML أو linting أو CI.

## 5. خطة التحسين ذات الأولوية

| الأولوية | الإجراء | الأثر |
|---|---|---|
| P0 | السماح بتكبير الهاتف وإضافة دعم لوحة المفاتيح وARIA | تحسين الوصولية فورًا وتقليل مشكلات الاستخدام |
| P0 | إضافة README وترخيص مناسب ووصف لطريقة التشغيل | جعل المشروع قابلًا للفهم والمشاركة |
| P1 | فصل HTML وCSS وJavaScript وإزالة inline handlers | تحسين الصيانة والمراجعة وإعادة الاستخدام |
| P1 | توحيد حلقة رسم الجسيمات وإضافة `clear()` | تقليل استهلاك المعالج ومنع بقايا المؤثرات بين الجولات |
| P1 | إضافة اختبارات للحسابات وانتقالات الحالة | منع regressions عند تعديل قواعد اللعبة |
| P2 | تثبيت الاعتماديات أو إضافة fallback وSRI | زيادة الاعتمادية وتقليل مخاطر سلسلة التوريد |
| P2 | إضافة `prefers-reduced-motion` وملصقات وصولية | تحسين التجربة للمستخدمين ذوي الاحتياجات المختلفة |
| P3 | إضافة حفظ أفضل نتيجة محليًا وإحصاءات الجولة | زيادة إعادة اللعب دون الحاجة إلى خادم |
| P3 | نقل منطق النتيجة إلى خادم إذا أصبحت اللعبة تنافسية | منع التلاعب بالنتائج وتحسين الثقة |

## 6. حكم نهائي

**Hiding.ring مشروع نموذج أولي ناجح بصريًا وقابل للتشغيل، وليس بعدُ مشروعًا جاهزًا للتوسع أو الإنتاج التنافسي.** قيمته الأساسية حاليًا في سرعة التشغيل وفكرة اللعبة والتأثيرات التفاعلية. وأفضل خطوة تالية ليست إضافة مؤثرات جديدة، بل تحسين الأساس: الوصولية، التوثيق، فصل الكود، الاختبار، وإدارة الجسيمات. بعد تنفيذ تحسينات P0 وP1، سيصبح المشروع أسهل في الصيانة وأكثر ملاءمة للنشر العام.

## المراجع

[1]: https://github.com/muha-001/Hiding.ring "مستودع Hiding.ring على GitHub"

[2]: https://api.github.com/repos/muha-001/Hiding.ring "GitHub Repository API metadata for Hiding.ring"

[3]: https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles/button_role "MDN: button role and accessibility guidance"

[4]: https://developer.mozilla.org/en-US/docs/Web/HTML/Viewport_meta_tag "MDN: Viewport meta tag"

[5]: https://developer.mozilla.org/en-US/docs/Web/Security/Subresource_Integrity "MDN: Subresource Integrity"
